import React ,{useState}from 'react';
import SiteLayout from '../../layout/SiteLayout';

const Post = () => {
    return (
        <SiteLayout>
            <h2>[Post] login :   postId : </h2>
        </SiteLayout>
    );
};

export default Post;